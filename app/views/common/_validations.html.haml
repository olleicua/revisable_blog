- if record.errors.any?
  #error_explanation
    %h2
      = pluralize(record.errors.count, "error")
      prohibited this #{record.class.to_s.titleize.downcase} from being saved:
    %ul
      - record.errors.full_messages.each do |message|
        %li= message
